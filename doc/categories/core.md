The main entry point for the Flutter Classic Bluetooth plugin.

Use `FlutterClassicBluetooth` to access all Bluetooth Classic operations
including adapter management, device discovery, pairing, RFCOMM connections,
and server sockets.
