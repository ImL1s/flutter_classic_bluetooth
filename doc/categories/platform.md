Platform interface and method channel implementation.

These classes are used internally by the plugin and by platform-specific
implementations. Most developers should use `FlutterClassicBluetooth` instead.
