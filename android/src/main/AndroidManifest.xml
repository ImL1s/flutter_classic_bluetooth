<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.flutter_classic_bluetooth.flutter_classic_bluetooth">

  <!-- Legacy (API < 31) -->
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

  <!-- Android 12+ (API 31+) -->
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

  <!--
    `required="false"`. `uses-feature` merges with OR, so a library declaring
    this as required overrides an app that declares it optional — and Play then
    filters the app off every device without Bluetooth hardware. That is a
    decision for the app to make, not for a transport plugin one of its screens
    happens to use. An app that genuinely needs Bluetooth can declare it
    required itself.
  -->
  <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
</manifest>
